managerConfig:
  controllerManagerConfigYaml: |
    apiVersion: config.kueue.x-k8s.io/v1beta1
    kind: Configuration
    integrations:
      frameworks:
      - "batch/job"
      # Add others only if you have them installed:
      # - "kubeflow.org/mpijob"
      # - "jobset.x-k8s.io/jobset"
    # Keep the rest of your config or defaults here if needed
